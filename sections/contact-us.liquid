<style>
  .contact-us-{{ section.id}} {
    width: 100%;
    height: 100%;
    padding: 60px 0px;
  }

  .contact-us-{{ section.id}} .contact-us-content {
    display: flex;
    justify-content: center;
    column-gap: 24px;
  }

  .contact-us-{{ section.id}} .contact-us-title {
    color: #333;
    font-family: 'Open Sans';
    font-size: 52px;
    font-style: normal;
    font-weight: 700;
    line-height: normal;
    margin-bottom: 32px;
    text-align: center;
  }
  .contact-us-{{ section.id}} .title {
    color: #333;
    text-align: center;
    font-family: 'Open Sans';
    font-size: 24px;
    font-style: normal;
    font-weight: 700;
    line-height: normal;
  }

  .contact-us-{{ section.id}} .item {
    display: flex;
    flex-direction: column;
    justify-content: flex-start;
    width: 100%;
    background-color: #f8f8f8;
    height: 100%;
    border-radius: 10px;
    padding: 20px 45px 0px;
    height: auto;
    align-items: center;
  }
  .contact-us-{{ section.id}} .item img {
    width: 180px;
    height: auto;
    object-fit: cover;
    object-position: center;
    margin: auto;
  }

  @media (max-width: 1000px) {
    .contact-us-{{ section.id}} {
      width: 100%;
      height: 100%;
      padding: 60px 0px;
    }
    .contact-us-{{ section.id}} .contact-us-content {
      flex-direction: column;
      row-gap: 20px;
    }
    .contact-us-{{ section.id}} .contact-us-title {
      font-size: 24px;
    }
    .contact-us-{{ section.id}} .title {
      font-size: 18px;
    }
    .contact-us-{{ section.id}} .item img {
      width: 150px;
    }
  }
</style>
<div class='contact-us contact-us-{{ section.id}}'>
  <div class='contact-us-inner container'>
    <h2 class='contact-us-title'>{{ section.settings.title }}</h2>
    <div class='contact-us-content'>
      {%- for block in section.blocks -%}
        <a href='{{ block.settings.link }}' target='_blank' class='item'>
          <p class='title'>{{ block.settings.title }}</p>
          <img src='{{ block.settings.image |  image_url  }}' width='100%' height='100%' alt=''>
        </a>
      {%- endfor -%}
    </div>
  </div>
</div>

{% schema %}
{
  "name": "contact-us",
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "t:Heading"
    }
  ],
  "blocks": [
    {
      "type": "contact-item",
      "name": "contact-item",
      "settings": [
        {
          "type": "text",
          "id": "title",
          "label": "title"
        },
        {
          "type": "image_picker",
          "id": "image",
          "label": "image"
        },
        {
          "type": "text",
          "id": "link",
          "label": "link"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "contact-us"
    }
  ]
}
{% endschema %}
